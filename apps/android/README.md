// settings.gradle.kts lives in apps/android/.
//
// Run from apps/android/:
//   ./gradlew :protocol:test           -- JVM tests for the protocol
//   ./gradlew :app:assembleDebug       -- build the Android app
//   ./gradlew test                     -- run all JVM tests
//
// The first time you build, run `./gradlew` once to download the
// distribution described in gradle/wrapper/gradle-wrapper.properties.
// The gradle-wrapper.jar is committed to the repo; gradlew/gradlew.bat
// are NOT (they're generated on first invocation by the IDE or by
// running `gradle wrapper` once from a system Gradle install).
